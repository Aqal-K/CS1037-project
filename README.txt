# CS1037-project
Compilation: gcc huffman.c encode.c pqueue.c decode.c writehuf.c huffman_tree.c freqtable.c -o huffman
Usage: "./huffman [encode | decode] <input_file_path> <output_file_path>
    Note:   if the input/output file is in the same folder as the compiled program, then
            only the filename is required, no need to include path in front